THRESHOLD=0.50
MAX_FILE_MB=8
TOPK=3
ALLOWED_EXT=jpg,jpeg,png
CORS_ENABLED=false

# === MobileNetV3 (recognize) ===
ONNX_PATH=models/mobilenetv3_small.onnx
CLASSES_PATH=models/imagenet_classes.txt

# === YOLO Cat-Head (faces) ===
CAT_HEAD_ONNX=models/cat_head_model.onnx
CAT_HEAD_CLASSES=models/cat_head_classes.json

# Inferensi & aturan
IMG_SIZE=640
CONF_RAW=0.20
MIN_CONF=0.40
MID_CONF=0.50
HI_COUNT=0.75
HI_PRIORITY=0.80
MAX_DET=5

# Upload ROI -> Laravel storage endpoint
UPLOAD_TO_LARAVEL=true
LARAVEL_UPLOAD_URL=http://localhost:8000/api/storage/upload
LARAVEL_UPLOAD_TOKEN=dev-secret-token   # opsional, bisa kosong

# Opsional
PORT=5000

CONTENT_API_BASE=
CONTENT_API_TIMEOUT=12

REMOVEBG_CACHE_DIR=cache/remove-bg